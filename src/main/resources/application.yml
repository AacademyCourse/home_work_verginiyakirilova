spring:
  datasource:
    password:
    url: jdbc:mysql://127.0.0.1:3306/home_work_db?createDatabaseIfNotExist=true
    username: root
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create
    show-sql: true
